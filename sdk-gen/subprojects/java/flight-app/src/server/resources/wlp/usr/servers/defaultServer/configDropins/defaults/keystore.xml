<server description="Default Server">
    <keyStore id="defaultKeyStore" location="key.jks" password="${KEYSTORE_PASSWORD}" type="jks"/>
    <keyStore id="defaultTrustStore" location="/opt/java/openjdk/lib/security/cacerts" password="${TRUSTSTORE_PASSWORD}" type="jks"/>
</server>
